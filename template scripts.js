var empty = getField("");
var empty = getField("");
var empty = getField("");
var empty = getField("");


//switch
switch (empty) {
		case '':
			
			break;
		case '':
			
			break;
		case '':
			
			break;
		case '':
			
			break;
		default:
			
}
//close switch (empty)



//if...else
if (empty) {
  code1;
} else {
  code2;
}
//close if (empty)



//ternary operator (same as if-else, but requires the else)
condition ? code1 : code2;


//function
var empty = function(something){
	something * other;
};

//call the function
empty(something);


